import { Box } from '@mui/material';
import React from 'react';

const Profile = () => {
    return (
        <Box flex={4} p={2} marginLeft={2}>
            Profile
        </Box>
    );
};

export default Profile;